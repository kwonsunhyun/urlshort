CREATE TABLE urlshort (
                               id INT auto_increment  primary key,
                               url VARCHAR(200) NOT NULL,
                               cnt int(20) NOT NULL

);
